import React from 'react';
import PropTypes from 'prop-types';

const Favorites = () => {
  return (
    <div>
      Favoritesd
    </div>
  );
};

Favorites.propTypes = {

};

export default Favorites;
